<%= form_for([@conference,@day]) do |f| %>
  <% if @day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@day.errors.count, "error") %> prohibited this day from being saved:</h2>

      <ul>
      <% @day.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
  	<%= f.label :date, 'Date'%>
    <%= f.text_field :date %>
  </div>
  <div class="field">
    <%= f.label :displayName, 'Display name for Day' %><br />
    <%= f.text_field :displayName %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>