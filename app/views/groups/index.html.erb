<h1>Listing groups</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Day</th>
    <th>Starttime</th>
    <th>Visible</th>
    <th>Conference</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.title %></td>
    <td><%= group.day_id %></td>
    <td><%= group.starttime %></td>
    <td><%= link_to 'Show', conference_group_path(@conference, group) %></td>
    <td><%= link_to 'Edit', edit_conference_group_path(@conference, group) %></td>
    <td><%= link_to 'Destroy', conference_group_path(@conference, group), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Group', new_conference_group_path %>

<br />
<br />

<%= link_to 'Back', conferences_path %>
