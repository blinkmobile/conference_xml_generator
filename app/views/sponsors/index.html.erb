<h1>Listing sponsors</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Logourl</th>
    <th>Conference</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sponsors.each do |sponsor| %>
  <tr>
    <td><%= sponsor.name %></td>
    <td><%= link_to 'Show', conference_sponsor_path(@conference, sponsor) %></td>
    <td><%= link_to 'Edit', edit_conference_sponsor_path(@conference, sponsor) %></td>
    <td><%= link_to 'Destroy', conference_sponsor_path(@conference, sponsor), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sponsor', new_conference_sponsor_path %>
<br />
<br />

<%= link_to 'Back', conferences_path %>