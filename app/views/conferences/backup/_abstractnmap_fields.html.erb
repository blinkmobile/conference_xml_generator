<tr class="fields">
  <td class="field">
    <%= f.label :speaker_id %>
    <%= f.select :speaker_id, @conference.speakers.all.collect {|sp| [ sp.name, sp.id ] } %>
  </td>
    <td class="field">
        <%= f.hidden_field :_destroy %>
        <%= link_to_remove_fields "remove", f %>
    </td>
</tr>