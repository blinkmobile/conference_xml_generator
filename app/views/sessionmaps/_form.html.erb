<%= form_for(@sessionmap) do |f| %>
  <% if @sessionmap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sessionmap.errors.count, "error") %> prohibited this sessionmap from being saved:</h2>

      <ul>
      <% @sessionmap.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :session_id %><br />
    <%= f.text_field :session_id %>
  </div>
  <div class="field">
    <%= f.label :speaker_id %><br />
    <%= f.text_field :speaker_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
