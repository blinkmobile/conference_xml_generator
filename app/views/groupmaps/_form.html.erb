<%= form_for(@groupmap) do |f| %>
  <% if @groupmap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@groupmap.errors.count, "error") %> prohibited this groupmap from being saved:</h2>

      <ul>
      <% @groupmap.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br />
    <%= f.text_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :speaker_id %><br />
    <%= f.text_field :speaker_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
